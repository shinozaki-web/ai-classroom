# AI教室 ローカル保存化セキュリティレビュー

実施日: 2026-08-24

## 対象

- `shared.js`
- `index.html`
- `teacher.html`
- Chrome `localStorage` と保存コードによる進捗保存

## 確認結果

- Supabase SDK、Project URL、公開キー、テーブル操作、Realtime接続を対象コードから除去した。
- 生徒プロフィール・授業進捗・バッジ・授業別の途中状態は、従来と同じ `localStorage` キーを使用するため既存端末のデータを引き継ぐ。
- 新規端末では名前と任意のクラス名だけを登録し、個人名簿を公開リポジトリへ配置しない。
- 入力した名前・クラス名は文字数を制限し、表示には `textContent` またはURLエンコードを使用する。
- 保存コードは既存形式との互換性を維持し、外部サーバーへ送信しない。
- 先生画面から固定パスワード、クラウド名簿操作、進捗削除、Realtime監視を除去した。
- 新しい先生画面は現在のChrome内の1人分だけを表示し、DOM生成時に `textContent` を使用する。
- 端末バックアップは利用者操作時だけJSONとしてローカルダウンロードし、外部通信を行わない。
- Supabase上の `students` 5件、`student_progress` 6件をリポジトリ外へ権限600でバックアップした。

## プライバシーと運用上の注意

- 同じChromeプロフィールを使える人は、その端末に保存された名前と進捗を閲覧できる。共用端末ではOSまたはChromeプロフィールを生徒ごとに分ける。
- Chromeの「サイトデータ」を消すと進捗も消える。各端末で保存コードまたは先生画面のJSONバックアップを定期的に保存する。
- 保存コードとJSONには名前・クラス・進捗が含まれるため、公開場所や不特定多数のチャットへ貼らない。
- 別PC・別Chromeプロフィールへの自動同期と、先生による全生徒の一括監視は行われない。
- Supabase停止前に本番URLで、既存データ保持、新規登録、再読み込み、授業遷移、保存コード復元を確認する。

## 判定

同じ生徒が同じPC・同じChromeプロフィールを継続利用する運用、および上記バックアップ手順を前提として本番公開可能。
